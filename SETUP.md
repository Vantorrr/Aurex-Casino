# 🚀 Быстрый старт Vantor Casino

## 📋 Предварительные требования

1. **Node.js** (версия 18 или выше)
   ```bash
   node --version  # Должно показать v18.x.x или выше
   ```

2. **MongoDB** (версия 5.0 или выше)
   - [Скачать MongoDB](https://www.mongodb.com/try/download/community)
   - Или использовать MongoDB Atlas (облачная версия)

3. **Git**
   ```bash
   git --version
   ```

## ⚡ Быстрая установка

### Шаг 1: Клонирование и установка
```bash
# Клонируем репозиторий
git clone <ваш-репозиторий-url>
cd vantor-casino

# Устанавливаем все зависимости одной командой
npm run install-all
```

### Шаг 2: Настройка переменных окружения

**Backend (.env файл в папке backend/):**
```env
NODE_ENV=development
PORT=6000
FRONTEND_URL=http://localhost:3000
MONGODB_URI=mongodb://localhost:27017/vantor-casino
JWT_SECRET=vantor-casino-super-secret-key-2024
JWT_EXPIRE=7d

# B2B Slots API (замените на ваши данные)
SLOTS_API_BASE_URL=https://int.apichannel.cloud
SLOTS_API_FALLBACK_URL=https://icdnchannel.com
OPERATOR_ID=0
CALLBACK_URL=https://api.vantorgame.site/api/

# Lava Top Payment (замените на ваши данные)
LAVA_TOP_API_URL=https://dev.lava.ru/
LAVA_TOP_SHOP_ID=your-shop-id
LAVA_TOP_API_KEY=your-api-key
```

**Frontend (.env.local файл в папке frontend/):**
```env
NEXT_PUBLIC_API_URL=http://localhost:6000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Vantor Casino
```

### Шаг 3: Запуск MongoDB

**Windows:**
```bash
mongod
```

**macOS (через Homebrew):**
```bash
brew services start mongodb-community
```

**Ubuntu/Debian:**
```bash
sudo systemctl start mongod
```

### Шаг 4: Инициализация базы данных
```bash
# Создаём тестовые данные
npm run seed
```

### Шаг 5: Запуск приложения
```bash
# Запуск всего проекта (backend + frontend)
npm run dev
```

## 🌐 Доступ к приложению

После запуска вы можете получить доступ к:

- **Сайт казино**: http://localhost:3000
- **API Backend**: http://localhost:6000
- **Админ панель**: http://localhost:3000/admin

## 🔑 Тестовые аккаунты

После выполнения `npm run seed` будут созданы следующие аккаунты:

### Администратор
- **Email**: admin@vantorcasino.com
- **Пароль**: admin123456
- **Роль**: Администратор (полный доступ)

### Демо пользователь
- **Email**: demo@vantorcasino.com
- **Пароль**: demo123456
- **Баланс**: 10,000₽

### VIP игрок
- **Email**: vip@vantorcasino.com
- **Пароль**: vip123456
- **VIP уровень**: 7
- **Баланс**: 250,000₽

## 🎮 Функциональность

### Для игроков:
- ✅ Регистрация и авторизация
- ✅ Пополнение и вывод средств
- ✅ Игра в слоты (интеграция с API)
- ✅ Турниры и лидерборды
- ✅ VIP система (10 уровней)
- ✅ Бонусы и промокоды
- ✅ Реферальная программа

### Для администраторов:
- ✅ Панель управления
- ✅ Управление пользователями
- ✅ Финансовая отчётность
- ✅ Мониторинг игровых сессий
- ✅ Система настроек

## 🔧 Конфигурация слотов

### Подключение к вашему провайдеру:

1. Откройте `backend/routes/slotsApi.js`
2. Измените настройки API в конструкторе `SlotsApiService`
3. Обновите эндпоинты колбэков согласно документации провайдера

### Пример настройки:
```javascript
// В backend/routes/slotsApi.js
class SlotsApiService {
  constructor() {
    this.baseUrl = 'https://ваш-провайдер-api.com';
    this.operatorId = 'ваш-operator-id';
    this.callbackUrl = 'https://ваш-домен.com/api/slots/callback';
  }
}
```

## 💳 Настройка платежей

### Lava Top интеграция:

1. Зарегистрируйтесь на [Lava Top](https://lava.ru/)
2. Получите `SHOP_ID` и `API_KEY`
3. Обновите `.env` файл в backend:
```env
LAVA_TOP_SHOP_ID=ваш-shop-id
LAVA_TOP_API_KEY=ваш-api-key
```

## 🛠️ Разработка

### Команды для разработки:
```bash
# Запуск в режиме разработки
npm run dev

# Запуск только backend
npm run server:dev

# Запуск только frontend
npm run client:dev

# Сборка для продакшена
npm run build

# Запуск в продакшене
npm run start
```

### Структура проекта:
```
vantor-casino/
├── backend/                 # Node.js + Express API
│   ├── config/             # Конфигурация
│   ├── models/             # MongoDB модели
│   ├── routes/             # API эндпоинты
│   ├── middleware/         # Middleware функции
│   └── server.js           # Главный сервер
├── frontend/               # Next.js приложение
│   ├── components/         # React компоненты
│   ├── pages/              # Страницы сайта
│   ├── hooks/              # Пользовательские хуки
│   ├── store/              # Управление состоянием
│   └── styles/             # CSS стили
└── scripts/                # Вспомогательные скрипты
```

## 🔍 Отладка

### Проблемы с подключением к MongoDB:
```bash
# Проверьте статус MongoDB
sudo systemctl status mongod  # Linux
brew services list | grep mongo  # macOS

# Проверьте подключение
mongo --eval "db.adminCommand('ismaster')"
```

### Проблемы с портами:
```bash
# Проверьте какие порты заняты
lsof -i :6000  # Backend
lsof -i :3000  # Frontend
```

### Логи приложения:
- Backend логи отображаются в терминале
- Frontend логи в браузере (F12 → Console)

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте, что MongoDB запущен
2. Убедитесь, что порты 3000 и 6000 свободны
3. Проверьте `.env` файлы
4. Посмотрите логи в терминале

## 🚀 Готово к запуску!

После выполнения всех шагов ваше казино будет полностью функциональным и готовым для тестирования или дальнейшей разработки!

**Удачи в создании лучшего онлайн казино! 🎰**